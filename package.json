{
  "name": "gma-lockers-saas",
  "version": "1.0.0",
  "description": "GMA Lockers SaaS — Sistema B2B para administración de casilleros",
  "private": true,
  "workspaces": [
    "shared",
    "cloud-engine",
    "mobile-app",
    "admin-desktop"
  ],
  "scripts": {
    "cloud:dev": "npm run dev --workspace=cloud-engine",
    "cloud:build": "npm run build --workspace=cloud-engine",
    "cloud:start": "npm run start --workspace=cloud-engine",
    "mobile:start": "npm run start --workspace=mobile-app",
    "desktop:dev": "npm run start --workspace=admin-desktop",
    "desktop:build": "npm run build --workspace=admin-desktop"
  },
  "author": "Anderson Garcia (GMA)",
  "license": "UNLICENSED",
  "dependencies": {
    "concurrently": "^10.0.4",
    "date-fns": "^4.4.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
